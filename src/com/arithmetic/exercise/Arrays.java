package com.arithmetic.exercise;

/*
 * 数组
 */
public class Arrays {
	/*
	 * 283.给定一个数组，使得非0的数排在前面，0排在后面，且非0的数相对位置不变
	 * 27.给定一个数组和一个数值，将所有与数值相等的数删除，返回剩余个数
	 * 26.给定一个有序数组，对数组中的元素去重，每个元素只有一个。返回去重后的长度
	 * 80.给定一个有序数组，对数组中的元素去重，每个元素只有二个。返回去重后的长度
	 */
		
}
